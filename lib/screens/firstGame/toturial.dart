import 'package:flutter/material.dart';
import '../../var.dart';

class Toturial_first extends StatelessWidget {
  const Toturial_first({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: homescreen ,
      body: const Center (child : Text ("DELETE CENTER WEDGIT AND START CODE" , style : TextStyle(color : Colors.white)))
    );
  }
}